{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"trnrand","text":"<p>Random number generation for AWS Trainium via NKI (Neuron Kernel Interface).</p> <p>Seeded pseudo-random distributions, low-discrepancy quasi-random sequences for quasi-Monte Carlo, and an on-device Philox RNG targeting the GpSimd engine \u2014 on-device generation avoids host\u2192device transfer for large random tensors.</p> <p>Part of the trnsci scientific computing suite (github.com/trnsci).</p>"},{"location":"#why","title":"Why","text":"<p>Reproducible, parallel-friendly RNG is a prerequisite for scientific computing on Trainium: weight initialization, noise injection, stochastic trace estimation, and Monte Carlo integration all depend on it. PyTorch's host-side <code>torch.Generator</code> works but pays the host\u2192device transfer cost for every sample. trnrand targets a stateless, counter-based RNG (Philox 4\u00d732) that runs on the GpSimd engine so large tensors are generated where they're consumed.</p>"},{"location":"#primary-use-cases","title":"Primary use cases","text":"<ul> <li>Noise injection for speech training (consumed by <code>trnfft</code>).</li> <li>Stochastic trace estimation and Monte Carlo integration (consumed by   <code>trnblas</code> for DF-MP2 quantum chemistry workloads).</li> <li>Truncated-normal weight initialization for neural-net layers.</li> </ul> <p>See Architecture for the Philox/GpSimd story.</p>"},{"location":"#related-projects","title":"Related projects","text":"<ul> <li>trnfft \u2014 FFT + complex ops for Trainium.</li> <li>trnblas \u2014 BLAS operations for Trainium.</li> <li><code>trnsolver</code> (planned) \u2014 Linear solvers and eigendecomposition.</li> </ul>"},{"location":"architecture/","title":"Architecture","text":""},{"location":"architecture/#layout","title":"Layout","text":"<pre><code>trnrand/\n\u251c\u2500\u2500 trnrand/\n\u2502   \u251c\u2500\u2500 __init__.py        # Re-exports all RNG operations\n\u2502   \u251c\u2500\u2500 generator.py       # Generator class, seeding, state management\n\u2502   \u251c\u2500\u2500 distributions.py   # uniform, normal, exponential, bernoulli, etc.\n\u2502   \u251c\u2500\u2500 quasi.py           # sobol, halton, latin_hypercube\n\u2502   \u2514\u2500\u2500 nki/\n\u2502       \u251c\u2500\u2500 __init__.py    # Backend dispatch (set_backend / HAS_NKI)\n\u2502       \u2514\u2500\u2500 dispatch.py    # Philox kernel scaffold for on-device RNG\n\u251c\u2500\u2500 tests/\n\u251c\u2500\u2500 examples/mc_integration.py\n\u2514\u2500\u2500 benchmarks/\n</code></pre>"},{"location":"architecture/#use-cases-across-the-suite","title":"Use cases across the suite","text":"Use case trnrand function Consumer Noise injection (speech training) <code>normal()</code> trnfft Stochastic trace estimation <code>normal()</code>, <code>sobol()</code> trnsolver Weight initialization <code>truncated_normal()</code> trnfft/nn.py Monte Carlo integration <code>sobol()</code>, <code>halton()</code> trnblas (DF-MP2) Hyperparameter sweeps <code>sobol()</code> Ablation studies Data augmentation <code>uniform()</code>, <code>bernoulli()</code> General"},{"location":"architecture/#nki-strategy","title":"NKI strategy","text":"<p>The Philox 4\u00d732 counter-based RNG maps cleanly to Trainium:</p> <ul> <li>GpSimd engine runs the integer multiply-XOR rounds (the Tensor Engine   is wasted on this).</li> <li>Parallel generation: each tile gets a disjoint counter range, no   cross-tile coordination required.</li> <li>Deterministic: <code>(counter, key) \u2192 output</code> \u2014 no state to synchronize   across cores.</li> </ul> <p>Philox is preferred over Mersenne Twister precisely because it's stateless and trivially parallelizable. It's the same engine used by cuRAND and JAX.</p>"},{"location":"architecture/#box-muller-for-normal","title":"Box-Muller for <code>normal()</code>","text":"<p>The on-device normal path is a Box-Muller transform layered on the Philox uniform stream:</p> <ul> <li>Pairs of uniforms <code>(u1, u2)</code> \u2192 standard-normal pairs <code>(z1, z2)</code> via   <code>r = \u221a(-2 ln u1)</code>, <code>\u03b8 = 2\u03c0 u2</code>, <code>z1 = r cos \u03b8</code>, <code>z2 = r sin \u03b8</code>.</li> <li>Runs on the Vector Engine, which has hardware <code>cos</code>/<code>sin</code>/<code>log</code>/<code>sqrt</code>.</li> <li>Box-Muller is preferred over Marsaglia polar here: Marsaglia avoids the   trig calls but uses rejection sampling, which serializes branch-divergent   lanes and kills SIMD throughput. Box-Muller has constant work per pair.</li> </ul>"},{"location":"architecture/#known-gaps","title":"Known gaps","text":"<ul> <li>NKI Philox kernel awaits on-hardware validation. The CPU reference   (<code>philox4x32_reference</code>, <code>philox_uniform_cpu</code>) is the conformance oracle;   see <code>tests/test_nki_philox.py::TestPhiloxNKI</code>. Tracked as #1.</li> <li>Box-Muller kernel awaits on-hardware validation. Tracked as #2; same   CPU-reference conformance pattern.</li> <li>Halton degrades above ~20 dimensions \u2014 known algorithmic limitation.   Sobol is preferred for <code>d &gt; 10</code>.</li> <li>Quasi-random sequences are host-only. NKI scrambling for Sobol/Halton   is a v0.3 follow-up.</li> </ul>"},{"location":"aws_setup/","title":"AWS Setup for Neuron Tests","text":"<p>To run <code>pytest -m neuron</code> against real Trainium hardware, we use a local workflow:</p> <ul> <li>Provision a Trainium EC2 instance with Terraform (stays stopped when not testing)</li> <li>Run the test script locally from your machine, using <code>AWS_PROFILE=aws</code></li> <li>The script starts the instance, runs pytest via SSM, prints output, stops the instance</li> </ul> <p>GitHub Actions does not touch AWS. All AWS interaction is human-initiated.</p>"},{"location":"aws_setup/#one-time-setup","title":"One-time setup","text":""},{"location":"aws_setup/#1-provision-the-ci-instance","title":"1. Provision the CI instance","text":"<p>Pick a VPC + subnet in a region with trn1/trn2/inf2 capacity. <code>trn1.2xlarge</code> is cheapest for basic validation.</p> <pre><code>cd infra/terraform\n\nAWS_PROFILE=aws terraform init\nAWS_PROFILE=aws terraform apply \\\n  -var=\"vpc_id=vpc-xxxxxx\" \\\n  -var=\"subnet_id=subnet-xxxxxx\" \\\n  -var=\"instance_type=trn1.2xlarge\"\n</code></pre> <p>Capture <code>instance_id</code> from the outputs. User-data takes ~5 minutes to install the Neuron SDK and clone trnrand.</p> <p>Stop the instance once ready:</p> <pre><code>AWS_PROFILE=aws aws ec2 stop-instances --instance-ids $(terraform output -raw instance_id)\n</code></pre>"},{"location":"aws_setup/#running-neuron-tests","title":"Running neuron tests","text":"<pre><code>AWS_PROFILE=aws ./scripts/run_neuron_tests.sh\n# or for trn2 / inf2:\nAWS_PROFILE=aws ./scripts/run_neuron_tests.sh trn2\n</code></pre> <p>The script will:</p> <ol> <li>Look up the tagged instance (<code>Name=trnrand-ci-trn1</code> by default)</li> <li>Start it if stopped; wait for SSM agent</li> <li>Send the pytest command over SSM</li> <li>Print stdout/stderr</li> <li>Stop the instance in a trap (even if pytest fails or you Ctrl-C)</li> </ol> <p>It exits non-zero if any test fails.</p>"},{"location":"aws_setup/#running-the-df-mp2-bench","title":"Running the DF-MP2 bench","text":"<p>Same instance, same SSM mechanism, runs <code>examples/df_mp2.py --bench</code> to capture per-step timing across small / medium / large synthetic shapes:</p> <pre><code>AWS_PROFILE=aws ./scripts/run_df_mp2_bench.sh                 # all 3 shapes\nAWS_PROFILE=aws ./scripts/run_df_mp2_bench.sh --shape medium  # one shape\n</code></pre> <p>Each shape runs cold then warm in the same Python process, so NEFF cache effects are visible in the reported numbers.</p>"},{"location":"aws_setup/#cost","title":"Cost","text":"<p>Stopped = EBS only (~$10/mo for 100 GB gp3). Running:</p> Type Hourly Typical run (10 min) trn1.2xlarge $1.34 $0.22 trn2.8xlarge $10.00 $1.67 inf2.xlarge $0.76 $0.13"},{"location":"aws_setup/#troubleshooting","title":"Troubleshooting","text":"<p>\"No instance found with Name=trnrand-ci-trn1\" \u2014 Run <code>terraform apply</code> first, or check that the tag matches.</p> <p>SSM <code>InvalidInstanceId</code> error \u2014 Instance hasn't finished booting/registering. Wait 1-2 minutes and retry.</p> <p>User-data didn't finish (<code>neuronxcc not found</code>) \u2014 SSH in via SSM session and re-run manually: <pre><code>aws ssm start-session --target $INSTANCE_ID\ncd /home/ubuntu/trnrand &amp;&amp; pip install -e '.[neuron,dev]'\n</code></pre></p> <p><code>InsufficientInstanceCapacity</code> when starting the instance \u2014 AWS may temporarily be out of Trainium in that AZ. Wait and retry, or re-provision in a different AZ.</p>"},{"location":"benchmarks/","title":"Benchmarks","text":"<p>trnrand kernels measured against two baselines on the same Trainium instance:</p> <ol> <li>NKI \u2014 trnrand with <code>set_backend(\"nki\")</code> (Philox on GpSimd; Box-Muller    on Vector Engine for <code>normal()</code>).</li> <li>trnrand-PyTorch \u2014 trnrand with <code>set_backend(\"pytorch\")</code> running on    the host CPU (<code>torch.Generator</code> + <code>torch.empty(...).uniform_()</code> etc.).</li> <li>torch.* \u2014 vanilla <code>torch.empty(n).uniform_(generator=g)</code> /    <code>torch.quasirandom.SobolEngine(...)</code> on the host CPU.</li> </ol> <p>The first comparison (1 vs 2) answers \"did the NKI Philox kernel actually help vs our own PyTorch fallback?\" \u2014 it uses the same trnrand call site on both sides; only the backend dispatch differs.</p> <p>The second comparison (1 vs 3) answers \"what's the user-visible difference between trnrand and reaching for raw torch?\"</p>"},{"location":"benchmarks/#methodology","title":"Methodology","text":"<ul> <li>Hardware: AWS <code>trn1.2xlarge</code> (1 NeuronCore-v2, 32 GB SBUF, AMD EPYC host CPU)</li> <li>Image: Deep Learning AMI Neuron PyTorch 2.9 (Ubuntu 24.04)</li> <li>Neuron SDK: <code>neuronxcc 2.24.5133.0</code></li> <li>Tool: pytest-benchmark with default settings (calibration + 5+ rounds, reports median)</li> <li>NKI warmup: each NKI test does an explicit warmup call before the timed loop so kernel compilation cost is excluded from steady-state numbers</li> <li>Reproduce: <code>AWS_PROFILE=aws ./scripts/run_benchmarks.sh</code></li> </ul>"},{"location":"benchmarks/#caveats","title":"Caveats","text":"<ul> <li>Small sizes are dispatch-bound. NKI kernel invocation has fixed overhead. Below some per-op threshold the host CPU wins for RNG, where each call already amortizes to a few hundred nanoseconds per element.</li> <li>Philox is integer-heavy. GpSimd does the multiply-XOR rounds; the Tensor Engine isn't useful here. Don't expect Tensor-Engine-class speedups \u2014 the win comes from avoiding host\u2192device transfer of the random tensor.</li> <li>Quasi-random is host-only today. Sobol/Halton/LHS run via <code>torch.quasirandom</code> on the host. NKI scrambling is a v0.3 follow-up.</li> <li>FP32 throughout. BF16/FP16 paths are future work.</li> <li>Numbers vary 5-15% run-to-run; treat the table as approximate.</li> </ul>"},{"location":"benchmarks/#results","title":"Results","text":"<p>Pending first run on trn1.2xlarge \u2014 see issue #3.</p>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#from-pypi","title":"From PyPI","text":"<pre><code>pip install trnrand\n</code></pre>"},{"location":"installation/#with-neuron-hardware-support","title":"With Neuron hardware support","text":"<pre><code>pip install trnrand[neuron]\n</code></pre> <p>This pulls in <code>neuronxcc</code> and <code>torch-neuronx</code>, which are only needed on Trainium/Inferentia instances. On CPU or GPU, trnrand falls back to <code>torch.Generator</code> automatically.</p> <p><code>neuronxcc</code> is not on public PyPI; it ships with the Deep Learning AMI Neuron. We pin <code>neuronxcc&gt;=2.24</code> to match the NKI calling convention used by sibling repos (see <code>pyproject.toml</code>).</p>"},{"location":"installation/#from-source","title":"From source","text":"<pre><code>git clone https://github.com/trnsci/trnrand\ncd trnrand\npip install -e \".[dev]\"\npytest tests/ -v\n</code></pre>"},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Python \u2265 3.10</li> <li><code>torch &gt;= 2.1</code></li> <li><code>numpy &gt;= 1.24</code></li> <li><code>neuronxcc &gt;= 2.24</code> (optional, for on-hardware NKI kernels)</li> </ul>"},{"location":"quickstart/","title":"Quickstart","text":"<pre><code>import trnrand\n\n# Seeded, reproducible generation\ng = trnrand.Generator(seed=42)\n\n# Standard distributions\nx = trnrand.normal(1000, mean=0.0, std=1.0, generator=g)\nu = trnrand.uniform(1000, low=-1.0, high=1.0, generator=g)\ne = trnrand.exponential(1000, rate=2.0, generator=g)\nb = trnrand.bernoulli(1000, p=0.3, generator=g)\n\n# Quasi-random sequences (better convergence for MC integration)\nsobol_pts = trnrand.sobol(1024, n_dims=5, seed=42)\nhalton_pts = trnrand.halton(1024, n_dims=3)\nlhs_pts = trnrand.latin_hypercube(100, n_dims=4)\n\n# Module-level seeding\ntrnrand.manual_seed(42)\nx = trnrand.standard_normal(256)\n</code></pre>"},{"location":"quickstart/#backend-selection","title":"Backend selection","text":"<pre><code>import trnrand\n\ntrnrand.set_backend(\"auto\")     # NKI on Trainium, PyTorch elsewhere (default)\ntrnrand.set_backend(\"pytorch\")  # force PyTorch\ntrnrand.set_backend(\"nki\")      # force NKI (requires Neuron hardware)\n</code></pre>"},{"location":"quickstart/#mc-vs-qmc-example","title":"MC vs QMC example","text":"<pre><code>python examples/mc_integration.py\n</code></pre> <p>Compares pseudo-random vs Sobol quasi-random for estimating the volume of a 5-D hypersphere. QMC converges O(1/N) vs O(1/\u221aN).</p>"},{"location":"release/","title":"Release procedure","text":"<p>trnrand publishes to PyPI via the GitHub Actions <code>publish.yml</code> workflow, using PyPI trusted publishers (OIDC) \u2014 no API tokens stored anywhere. Pre-releases (tags containing <code>-</code>, e.g. <code>v0.2.0-rc1</code>) go to TestPyPI; stable tags go to PyPI.</p>"},{"location":"release/#one-time-setup","title":"One-time setup","text":"<ol> <li>Reserve the PyPI name. Register <code>trnrand</code> on    PyPI and TestPyPI by    creating a placeholder release manually, or by configuring the trusted    publisher first and letting the first OIDC publish create the project.</li> <li>Configure trusted publisher \u2014 for both PyPI and TestPyPI:</li> <li>Go to <code>Account \u2192 Publishing \u2192 Add a new pending publisher</code>.</li> <li>Owner: <code>scttfrdmn</code>, repository: <code>trnrand</code>, workflow: <code>publish.yml</code>,      environment: <code>pypi</code> (PyPI) or <code>testpypi</code> (TestPyPI).</li> <li>Confirm the GitHub environments exist. In the repo settings under    <code>Environments</code>, ensure <code>pypi</code> and <code>testpypi</code> are present (the workflow    references them via <code>environment.name</code>).</li> </ol>"},{"location":"release/#cutting-a-release","title":"Cutting a release","text":"<ol> <li>Bump version in <code>pyproject.toml</code> and add a CHANGELOG section under    <code>## [Unreleased]</code> \u2192 move it to <code>## [X.Y.Z] - YYYY-MM-DD</code>.</li> <li>Commit the bump on <code>main</code> (or via PR).</li> <li>Tag and create a GitHub release:    <pre><code>git tag vX.Y.Z\ngit push origin vX.Y.Z\ngh release create vX.Y.Z --generate-notes\n</code></pre>    For a release candidate, use <code>vX.Y.Z-rc1</code> \u2014 the workflow routes that to    TestPyPI.</li> <li>Verify the workflow run on the Actions tab. PyPI/TestPyPI shows the    new version within a minute or two.</li> </ol>"},{"location":"release/#pre-release-smoke-test","title":"Pre-release smoke test","text":"<p>Before cutting a stable release, do a TestPyPI dry-run:</p> <pre><code>git tag v0.1.0-rc1\ngit push origin v0.1.0-rc1\ngh release create v0.1.0-rc1 --prerelease --generate-notes\n\n# After the workflow lands, verify the install works from TestPyPI:\npip install --index-url https://test.pypi.org/simple/ \\\n            --extra-index-url https://pypi.org/simple/ \\\n            trnrand==0.1.0rc1\npython -c \"import trnrand; print(trnrand.__version__)\"\n</code></pre>"},{"location":"release/#rollback","title":"Rollback","text":"<p>PyPI does not allow re-uploading the same version. If a release is broken, yank it (<code>pip</code> will refuse to install yanked versions but they remain installable when explicitly pinned for reproducibility) and cut a new patch version:</p> <pre><code># from your PyPI account UI: yank the broken version\ngit tag vX.Y.(Z+1)\ngit push origin vX.Y.(Z+1)\ngh release create vX.Y.(Z+1) --generate-notes\n</code></pre>"},{"location":"api/distributions/","title":"Distributions","text":"<p>All distribution functions accept a positional <code>*size</code> argument and an optional <code>generator=</code> for deterministic streams. When <code>generator</code> is omitted, the module-level default generator is used.</p>"},{"location":"api/distributions/#uniformsize-low00-high10-dtypetorchfloat32-generatornone","title":"<code>uniform(*size, low=0.0, high=1.0, dtype=torch.float32, generator=None)</code>","text":"<p>Uniform distribution on <code>[low, high)</code>.</p>"},{"location":"api/distributions/#normalsize-mean00-std10-dtypetorchfloat32-generatornone","title":"<code>normal(*size, mean=0.0, std=1.0, dtype=torch.float32, generator=None)</code>","text":"<p>Normal (Gaussian) distribution <code>N(\u03bc, \u03c3\u00b2)</code>.</p>"},{"location":"api/distributions/#standard_normalsize-dtypetorchfloat32-generatornone","title":"<code>standard_normal(*size, dtype=torch.float32, generator=None)</code>","text":"<p>Shorthand for <code>normal(*size, mean=0.0, std=1.0, ...)</code>.</p>"},{"location":"api/distributions/#exponentialsize-rate10-dtypetorchfloat32-generatornone","title":"<code>exponential(*size, rate=1.0, dtype=torch.float32, generator=None)</code>","text":"<p>Exponential distribution with rate <code>\u03bb</code> (mean <code>1/\u03bb</code>). Samples via <code>-log(U) / \u03bb</code> with clamping to avoid <code>log(0)</code>.</p>"},{"location":"api/distributions/#bernoullisize-p05-dtypetorchfloat32-generatornone","title":"<code>bernoulli(*size, p=0.5, dtype=torch.float32, generator=None)</code>","text":"<p>Bernoulli distribution: 1 with probability <code>p</code>, 0 otherwise.</p>"},{"location":"api/distributions/#randintsize-low0-high1-dtypetorchlong-generatornone","title":"<code>randint(*size, low=0, high=1, dtype=torch.long, generator=None)</code>","text":"<p>Random integers from <code>[low, high)</code>.</p>"},{"location":"api/distributions/#randpermn-dtypetorchlong-generatornone","title":"<code>randperm(n, dtype=torch.long, generator=None)</code>","text":"<p>Random permutation of <code>0..n-1</code>.</p>"},{"location":"api/distributions/#truncated_normalsize-mean00-std10-low-20-high20-dtypetorchfloat32-generatornone","title":"<code>truncated_normal(*size, mean=0.0, std=1.0, low=-2.0, high=2.0, dtype=torch.float32, generator=None)</code>","text":"<p>Truncated normal clipped to <code>[mean + low\u00b7std, mean + high\u00b7std]</code> via rejection sampling. Useful for bounded weight initialization.</p>"},{"location":"api/generator/","title":"Generator","text":"<p>Reproducible seeding and state management. The <code>Generator</code> wraps <code>torch.Generator</code> for CPU/CUDA today; on Neuron hardware it will dispatch to a Philox counter-based RNG running on the GpSimd engine.</p>"},{"location":"api/generator/#generatorseednone-devicecpu","title":"<code>Generator(seed=None, device=\"cpu\")</code>","text":"<p>Construct a seeded generator. <code>seed=None</code> leaves the underlying <code>torch.Generator</code> unseeded (random state).</p>"},{"location":"api/generator/#generatormanual_seedseed-generator","title":"<code>Generator.manual_seed(seed) -&gt; Generator</code>","text":"<p>Re-seed in place. Returns <code>self</code> so you can chain.</p>"},{"location":"api/generator/#generatorget_state-set_statestate","title":"<code>Generator.get_state() / set_state(state)</code>","text":"<p>Save and restore the underlying <code>torch.Generator</code> state for checkpointing.</p>"},{"location":"api/generator/#generatortorch_generator","title":"<code>Generator.torch_generator</code>","text":"<p>Access the underlying <code>torch.Generator</code> for use with raw PyTorch ops.</p>"},{"location":"api/generator/#manual_seedseed-generator","title":"<code>manual_seed(seed) -&gt; Generator</code>","text":"<p>Module-level helper that re-seeds (and replaces) the default generator.</p>"},{"location":"api/generator/#get_default_generator-generator","title":"<code>get_default_generator() -&gt; Generator</code>","text":"<p>Return the module-level default generator used when <code>generator=None</code> is passed to a distribution function.</p>"},{"location":"api/nki/","title":"NKI Backend","text":"<p>The NKI dispatch layer controls whether RNG operations run on the native Trainium GpSimd engine (Philox 4\u00d732) or fall back to <code>torch.Generator</code>.</p>"},{"location":"api/nki/#backend-selection","title":"Backend selection","text":"<pre><code>import trnrand\n\ntrnrand.set_backend(\"auto\")     # NKI on Trainium, PyTorch elsewhere (default)\ntrnrand.set_backend(\"pytorch\")  # force PyTorch fallback\ntrnrand.set_backend(\"nki\")      # force NKI (requires neuronxcc)\n</code></pre> <p><code>trnrand.HAS_NKI</code> is <code>True</code> when <code>neuronxcc</code> is importable. <code>trnrand.get_backend()</code> returns the active backend name.</p>"},{"location":"api/nki/#philox-kernel","title":"Philox kernel","text":"<p>The NKI Philox kernel lives in <code>trnrand/nki/dispatch.py</code>. The strategy:</p> <ul> <li>Counter-based \u2014 <code>(counter, key) \u2192 output</code>, no shared state across tiles.</li> <li>Each tile gets a disjoint counter range and runs the multiply-XOR rounds   on the GpSimd engine.</li> <li>Same engine used by cuRAND and JAX.</li> </ul> <p>Status: scaffolded but not yet validated on trn1/trn2 hardware. All generation falls back to <code>torch.Generator</code> until the kernel ships. See the roadmap issues for on-hardware validation work.</p>"},{"location":"api/quasi/","title":"Quasi-random sequences","text":"<p>Low-discrepancy sequences for quasi-Monte Carlo integration. Convergence is <code>O(1/N)</code> instead of pseudo-random's <code>O(1/\u221aN)</code>, which is decisive for moderate-dimensional integrals.</p>"},{"location":"api/quasi/#soboln_points-n_dims-seednone-scrambletrue-dtypetorchfloat32","title":"<code>sobol(n_points, n_dims, seed=None, scramble=True, dtype=torch.float32)</code>","text":"<p>Sobol sequence backed by <code>torch.quasirandom.SobolEngine</code>. Owen scrambling is on by default for better equidistribution. Returns a <code>(n_points, n_dims)</code> tensor in <code>[0, 1)</code>.</p>"},{"location":"api/quasi/#haltonn_points-n_dims-dtypetorchfloat32","title":"<code>halton(n_points, n_dims, dtype=torch.float32)</code>","text":"<p>Halton sequence using the first <code>n_dims</code> primes as bases. Simpler than Sobol but degrades above ~20 dimensions \u2014 use Sobol for <code>d &gt; 10</code>. Returns a <code>(n_points, n_dims)</code> tensor in <code>(0, 1)</code>.</p>"},{"location":"api/quasi/#latin_hypercuben_points-n_dims-dtypetorchfloat32-generatornone","title":"<code>latin_hypercube(n_points, n_dims, dtype=torch.float32, generator=None)</code>","text":"<p>Latin Hypercube Sampling. Stratified \u2014 each row and column of the hypercube contains exactly one sample. Better space-filling than pure random for small sample sizes.</p>"}]}